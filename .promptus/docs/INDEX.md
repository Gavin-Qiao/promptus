# Findings — Promptus

> Distilled findings live as one file per unit in this directory
> (`.promptus/docs/<slug>.md`), written via `bun "${CLAUDE_PLUGIN_ROOT}/scripts/kb-add.ts" --substrate finding`.
> This index is a human entry point; the machine index is the derived
> `.promptus/cache/CATALOG.md` (rebuilt by `kb-index`).

| status | title | path |
|---|---|---|
| _none yet_ | | |
